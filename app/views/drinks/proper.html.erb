<% provide(:title, "Proper cocktails") %>

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
    <h2><strong>A proper cocktail has 3 ingredients and bitters</strong></h2>
    <hr>
      <% if @drinks.empty? %>
         <h1>There are no proper drinks! 🙊 </h1>
      <% else %>
        <% @drinks.each do |drink| %>
          <ul>
          <li><strong><%= link_to drink.name.titlecase, drink_path(drink) %></strong></li>
          </ul>
        <% end %>
      <% end %>
      <br>
      <%= link_to "Go Back",  :back, class: "btn btn-sm btn-primary" %>
    </section>
  </aside>
</div>



